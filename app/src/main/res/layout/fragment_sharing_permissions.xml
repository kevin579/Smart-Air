<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@android:color/white"> <!-- Ensure a clean background -->

    <!-- Top App Bar -->
    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="#1565C0"
        android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
        app:popupTheme="@style/ThemeOverlay.AppCompat.Light"
        app:title="My Children"
        app:titleTextColor="@android:color/white" />

    <!-- Title/Instruction TextView -->
    <TextView
        android:id="@+id/textView5"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:layout_marginBottom="16dp"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:text="Control What Your Provider Sees"
        android:textSize="22sp"
        android:textColor="@android:color/black"
        android:gravity="center_horizontal"
        android:textStyle="bold" />

    <!-- Scrollable Content Area -->
    <ScrollView
        android:id="@+id/scrollView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:scrollbarStyle="insideOverlay">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingStart="16dp"
            android:paddingEnd="16dp"
            android:paddingTop="8dp"
            android:paddingBottom="16dp">

            <!-- Switch Items (All use standard height and margins) -->

            <Switch
                android:id="@+id/rescuelogswitch"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:minHeight="48dp"
                android:text="Share Rescue Logs"
                android:textSize="18sp"
                android:textStyle="bold" />

            <Switch
                android:id="@+id/controlleradherenceswitch"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:minHeight="48dp"
                android:text="Share Controller Adherence"
                android:textSize="18sp"
                android:textStyle="bold" />

            <Switch
                android:id="@+id/sharesymptomsswitch"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:minHeight="48dp"
                android:text="Share Symptoms"
                android:textSize="18sp"
                android:textStyle="bold" />

            <Switch
                android:id="@+id/sharetriggersswitch"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:minHeight="48dp"
                android:text="Share Triggers"
                android:textSize="18sp"
                android:textStyle="bold" />

            <Switch
                android:id="@+id/sharepefswitch"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:minHeight="48dp"
                android:text="Share Peak-Flow (PEF)"
                android:textSize="18sp"
                android:textStyle="bold" />

            <Switch
                android:id="@+id/sharetriageswitch"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:minHeight="48dp"
                android:text="Share Triage Incidents"
                android:textSize="18sp"
                android:textStyle="bold" />

            <Switch
                android:id="@+id/sharechartsswitch"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:minHeight="48dp"
                android:text="Share Summary Charts"
                android:textSize="18sp"
                android:textStyle="bold" />

            <Switch
                android:id="@+id/shareinventoryswitch"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:minHeight="48dp"
                android:text="Share Inventory"
                android:textSize="18sp"
                android:textStyle="bold" />

            <!-- Add a small bottom margin/padding for scrolling clearance -->
            <View android:layout_width="match_parent" android:layout_height="32dp"/>

        </LinearLayout>
    </ScrollView>
</LinearLayout>